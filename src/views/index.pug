extends layout.pug

block content
  if session.username
      h3 hello #{session.username}
  else
      h3 Log in to your #{instanceUrl} account
      form(method="POST" action="/login")
          button(type='submit') Log in with pleroma
        
