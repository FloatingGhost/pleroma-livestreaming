server {
    listen 80;
    server_name floatingghost.stream.ihatebeinga.live;
    access_log /var/log/nginx/floatingghost.access.log;
    error_log /var/log/nginx/floatingghost.error.log;
    absolute_redirect off;
    include /etc/nginx/sites/*.conf;
}
